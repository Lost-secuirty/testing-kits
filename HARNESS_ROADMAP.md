# Harness Roadmap

Forward-looking companion to `HARNESS_INVENTORY.md`. The inventory documents what exists; this file documents shipped batches, known gaps, and cleanup work that should happen before more harness expansion.

## Current state

- Current documented inventory count: **92 harnesses**.
- Runtime dependency policy: repo-native tests are allowed when a target repo needs them; this reusable harness library remains standard-library-only unless a specific harness justifies otherwise.
- Proof model: TEETH is the proof-strength ratchet. Required harnesses declare TEETH and must prove the correct oracle is not flagged while planted mutants are caught; pending harnesses remain visible but are not overstated as fully proven; pharmacy legacy harnesses remain separately labelled.
- Latest documented main-state signal: Batch 10 records **84 required / 0 pending / 8 legacy / 0 failing** — the TEETH campaign is complete for all non-pharmacy harnesses (Batch 10 added 15 OWASP Top 10:2025 web + LLM harnesses). Verify with `make proof` before using this as a fresh release claim.
- Harness mapping: `docs/HARNESS_MAP.md` is a current-state explanatory map. Entries are subject to change as the repo grows and as harnesses ratchet from `pending` to `required`.
- Test shape: one self-contained harness file plus one paired `unittest` file; add a proof test when the paired test alone does not prove the harness catches a planted bad case.
- Status report policy: `STATUS.md` and `STATUS.json` are generated by `make report`; CI uploads them as artifacts. Do not treat a stale committed status file as canonical.

## Shipped history

### Batch 4 — shipped 2026-05-27

10 new harnesses, research-grounded against 2026 sources (CWE Top 25 2025, OWASP LLM Top 10 2025, ChaosAPI/PACMPL 2025, AI-coded-bug surveys, the Statsig postmortem of the Google June 2025 outage).

| # | Path | Direction | Source signal |
|---|---|---|---|
| 44 | `harnesses/core/null_propagation_test_harness.py` | Gap-fill | AI-coded null/None propagation failures |
| 45 | `harnesses/core/error_path_leak_test_harness.py` | Gap-fill | Error-path cleanup/resource leaks |
| 46 | `harnesses/core/feature_flag_test_harness.py` | Gap-fill | Feature-flag rollout/default failures |
| 47 | `harnesses/core/clock_skew_test_harness.py` | Gap-fill | Distributed-time bugs |
| 48 | `harnesses/core/schema_evolution_test_harness.py` | Gap-fill | Silent schema drift |
| 49 | `harnesses/core/dormant_code_test_harness.py` | Gap-fill | Untriggered branches first-hit crash |
| 50 | `harnesses/core/cardinality_test_harness.py` | Gap-fill | Metric/cache cardinality explosion |
| 51 | `harnesses/core/hermeticity_test_harness.py` | Gap-fill | Flaky tests from hidden environment dependence |
| 52 | `harnesses/pharmacy/drug_interaction_test_harness.py` | Pharmacy | Pharmacy-domain interaction oracle |
| 53 | `harnesses/ai/prompt_injection_test_harness.py` | AI/LLM | Prompt-injection and prompt-safety surface |

### Batch 5 — shipped 2026-05-29

6 harnesses.

| # | Path | Direction | Self-test |
|---|---|---|---|
| 54 | `harnesses/core/tracing_test_harness.py` | Gap-fill: observability | 22 scenarios |
| 55 | `harnesses/core/queue_test_harness.py` | Gap-fill: messaging | 20 scenarios |
| 56 | `harnesses/core/search_relevance_test_harness.py` | New vertical: IR/search | 22 scenarios |
| 57 | `harnesses/ai/rag_eval_test_harness.py` | AI/LLM deeper | 19 scenarios |
| 58 | `harnesses/core/graphql_test_harness.py` | Gap-fill: contract | 21 scenarios |
| 59 | `harnesses/core/payments_test_harness.py` | New vertical: commerce | 27 scenarios |

### Ported: resilience / auth-privacy harnesses — shipped 2026-05-29

| # | Path | Port | Self-test | Unit tests |
|---|---|---|---|---|
| 60 | `harnesses/core/circuitbreaker_test_harness.py` | 19330 | 13 | 17 |
| 61 | `harnesses/security/jwt_test_harness.py` | 19400 | 14 | 18 |
| 62 | `harnesses/security/pii_redaction_test_harness.py` | 19410 | 14 | 20 |

Ports were reassigned on the way in to avoid colliding with batch 5 reservations.

### Batch 6 — shipped 2026-05-29

5 harnesses, closing the previous deferred list: gRPC, browser/E2E, IoT, drift detection, and multi-turn agent eval.

| # | Path | Direction | Self-test | Unit tests |
|---|---|---|---|---|
| 63 | `harnesses/ai/agent_eval_test_harness.py` | AI/LLM: multi-turn agent eval | 23 | 20 |
| 64 | `harnesses/core/iot_telemetry_test_harness.py` | New vertical: IoT/telemetry | 24 | 22 |
| 65 | `harnesses/core/grpc_contract_test_harness.py` | Gap-fill: RPC contract | 23 | 26 |
| 66 | `harnesses/core/browser_e2e_test_harness.py` | Gap-fill: E2E surrogate | 22 | 23 |
| 67 | `harnesses/ai/drift_detection_test_harness.py` | AI/LLM: model/embedding drift | 24 | 17 |

Batch 6 added 116 self-test scenarios and 108 unit tests. These are in-process oracles with local fake clocks/event loops and hand-rolled math where relevant.

### Batch 7 — shipped 2026-06-09

5 harnesses focused on proof-backed security, agent, and game-test gaps. Each has a self-contained harness, paired unit test, and planted-bug proof test.

| # | Path | Direction | Proof target |
|---|---|---|---|
| 68 | `harnesses/security/cwe_kev_regression_test_harness.py` | Security: CWE / KEV regression classes | Safe fixtures pass; vulnerable fixtures fail |
| 69 | `harnesses/ai/agent_memory_context_test_harness.py` | AI/agent: memory and authority boundaries | Spoofed/poisoned context is blocked |
| 70 | `harnesses/core/game_loop_simulation_test_harness.py` | Game: deterministic tick-loop invariants | Planted engine bugs are caught |
| 71 | `harnesses/core/statistical_rng_oracle_test_harness.py` | Game/math: RNG and drop-table oracles | Biased RNG fails distribution checks |
| 72 | `harnesses/core/canvas_scene_state_test_harness.py` | Game/frontend: renderer-independent scene state | Bad scene data is rejected |

### Inventory reconciliation — shipped 2026-06-10

`harnesses/core/complexity_test_harness.py` existed with paired tests but was missing from the numbered inventory. It is now tracked as #73 and included in proof audit discovery.

### Batch 8 — ported from lostsoulfs repos — shipped 2026-06-13

4 harnesses ported after a cross-repo scan of `github.com/lostsoulfs` (slot-math, game, pharmacy, and portfolio/governance repos) for testable logic not already covered by the existing 73. Each is python-direct (no JS/TS reimplementation), stdlib-only, with a paired unittest and a planted-bug proof test.

| # | Path | Source | Proof target |
|---|---|---|---|
| 74 | `harnesses/security/ci_workflow_hardening_test_harness.py` | `tools/control_audit.py` (portfolio/journal/workbench) | Unpinned-action / pull_request_target / missing-permissions workflows are flagged; hardened one passes; naive auditor misses the unpinned action |
| 75 | `harnesses/core/check_digit_identifier_test_harness.py` | pharmacy-app `verify_dea_logic` | Single-digit corruption detected (Luhn/ISBN-10 100%, DEA = its derived blind set); naive validator accepts a corrupted id the real one rejects |
| 76 | `harnesses/security/diff_secret_gate_test_harness.py` | `tools/scan_staged.py` (secrets half only) | Removed-line secret does not trip; added-line secret flagged at the right line; naive whole-file scan over-reports |
| 77 | `harnesses/core/lexical_date_canonicalization_test_harness.py` | pharmacy-app `data.py` TEXT dates | Non-canonical date sorts wrong lexically; strict validator rejects it; canonicalization restores chronological ORDER BY |

All four bind no port (in-process oracles). Verified locally at the time: `proof_audit` reported 77/77 OK; the 4 harness self-tests and 8 new test modules (52 tests) passed. That wording is now historical: current proof language should use TEETH `required` / `pending` / `legacy`, not blanket "77/77 proven."

### TEETH ratchet — shipped 2026-06-14 onward

The repo moved from keyword/presence-based proof accounting to a ratcheting TEETH model.

- Batch 0 created the TEETH contract and moved 9 harnesses to required.
- Batch 1 moved 10 more to required.
- Batch 2 moved 10 more to required.
- Batch 3 moved 10 more to required.
- Batches 4–9 moved 30 more to required. Batch 9 records **69 required / 0 pending / 8 legacy / 0 failing** for the pre-OWASP-expansion inventory.
- Batch 10 (2026-06-21) added **15 new required** harnesses — OWASP Top 10:2025 web (`security/{crypto,misconfig,advanced_injection,supplychain_depth,security_logging,rate_limit,session,exceptional_conditions,ast_sast}`) and OWASP Top 10 for LLM Applications 2025 (`ai/{excessive_agency,insecure_output_handling,sensitive_disclosure,unbounded_consumption,secure_codegen_eval,prompt_ab}`).

Current language rule: "92 harnesses" describes inventory size. "84 required / 0 pending / 8 legacy / 0 failing" describes the latest documented proof-ratchet state and must be re-verified before release notes or external claims.

### Harness mapping — started 2026-06-17

Mapping is explanatory documentation, not command authority. It connects each harness to its failure class, logic shape, proof status, outside testing pattern, and limits.

- Mapping Batch 1 covers `core/stress`, `core/api`, `core/db`, `core/scraper`, and `core/cli` in `docs/HARNESS_MAP.md`.
- Mapping Batch 2 covers `security/security`, `core/chaos`, `core/memory`, `core/concurrency`, and `core/fuzz` in `docs/HARNESS_MAP.md`.
- Mapping Batch 3 covers `core/property`, `core/mutation`, `core/regression_snapshot`, `core/contract`, and `core/serialization` in `docs/HARNESS_MAP.md`.
- Some early mapping entries were written while individual harnesses still had `pending` status. Use live proof output and `docs/GOLDEN_STATS.md` before treating a mapping entry's status text as current.
- All mapping entries are subject to change as source, tests, TEETH status, or repo scope changes.

## Next-batch candidates

No new harness candidate is approved after Batch 10. The leading candidates are the deferred Tier-2/Tier-3 items from the 2026-06-13 lostsoulfs port scan, but the TEETH/pending ratchet and harness mapping docs are higher priority than adding inventory.

Deferred OWASP coverage (specific, reproducible gaps surfaced while shipping Batch 10 — noted, not built): OWASP Top 10:2025 web **A08 Software or Data Integrity Failures**; OWASP Top 10 for LLM Applications 2025 **LLM04 Data Poisoning**, **LLM07 System Prompt Leakage**, **LLM09 Misinformation**.

Add candidates here only after a gap is specific, reproducible, and not already covered by an existing harness.

### Exploratory proof-layer candidates — planned 2026-06-27

`docs/EXPLORATORY_PROOF_LAYER.md` defines a proposed upgrade track for generated-input and sequence-oriented checks. This track is not a claim that all combinations can be tested. It is a plan for adding compact, pure-stdlib harnesses that complement TEETH.

Candidate sequence:

1. `core/combinatorial_coverage_test_harness.py` — finite pairwise/t-way coverage accounting with planted coverage mutants.
2. `core/counterexample_replay_test_harness.py` — deterministic counterexample canonicalization and replay metadata.
3. `core/stateful_sequence_budget_test_harness.py` — bounded action-sequence exploration over tiny frozen state machines.
4. `core/boundary_corpus_expander_test_harness.py` — deterministic boundary expansion that preserves known-bad anchors.

Rules for this track:

- TEETH remains the proof anchor.
- Generated-input checks do not replace planted-bad fixtures.
- Any new counterexample must be frozen into deterministic evidence before it becomes a required proof claim.
- No runtime dependency is approved by this planning note.
- Do not mix exploratory harnesses with workflow, dashboard, dependency, branch-protection, or generated-status changes.

## Per-harness internal gaps

Known thinness that can be filled without creating another harness:

- Ratchet a legacy or newly added harness into TEETH required status only when it has a real oracle, planted mutants, a non-empty frozen/literal corpus where needed, and a self-test that cannot pass inert.
- `pharmacy/srs` — SM-2 correctness exists; multi-user leak/privacy surface remains thin.
- Add new entries only when a specific failure mode is found through audit, CI, or a local reproduction.

## Mapping-batch closeout rule

Every harness mapping batch must end with a docs closeout pass before the PR is marked ready.

Check and update, when affected:

- `HARNESS_INVENTORY.md`
- `HARNESS_ROADMAP.md`
- `docs/HARNESS_READING_GUIDE.md`
- `docs/DOCS_MAP.md`
- `docs/REVIEWER_QUICKSTART.md`
- `README.md`
- any new category or per-harness mapping docs

The closeout must confirm that harness counts, TEETH status, category placement, command names, and public proof wording have not gone stale.

## Operational gaps

- Known flaky localhost mock-server tests: `test_network_test_harness::test_post_echoes_path` and `test_api_test_harness::TestMockServerIntegration::test_update_nonexistent_404` have both been observed timing out under high CPU contention while passing in isolation. A readiness-wait/retry fix for the shared mock-server pattern should be its own narrow PR.
- Keep generated `STATUS.md` / `STATUS.json` out of git unless the status convention is explicitly revised.
- Re-check exact CI context names before changing branch protection; do not guess required-check names.

## Resolved cleanup notes

Previously tracked issues that should not keep appearing as active gaps:

- CLI contract fixes for `a11y`, `concurrency`, `mutation`, `numeric`, and `security`.
- `pharmacy/srs` overflow clamp at `INTERVAL_CAP`.
- `core/hermeticity` home-directory dependency detection on Windows.
- Windows portability for memory/process counters and UTF-8 stdout handling.
- Python 3.12 float-summation behavior in `core/numeric` handled with explicit `+=` accumulation.

## Hygiene backlog

Repo-level cleanup not tied to a single harness:

- Reconcile `HARNESS_INVENTORY.md`, `HARNESS_ROADMAP.md`, and proof audit discovery after each TEETH or mapping batch.
- Keep README proof language aligned with current gate names and proof terminology.
- Keep `docs/HARNESS_MAP.md` aligned with current proof status and source evidence after each mapping batch.
- Add category-level coverage/reporting if a dev dependency such as `coverage` is explicitly accepted.
- Consider a release tag only after documentation, CI state, and proof-ratchet counts are consistent.

# Journal — Claude (Claude Code sessions)

First-person continuity notes across sessions for this repo. Read this on session start;
append newest at top, don't rewrite history. Suspicions welcome — mark them as such. Durable
*tool* facts belong in the kb sheets; project gotchas in `docs/LEARNINGS.md`.

---

## 2026-06-13 — kb scaffold added (cross-repo governance rollout)
Adopted the portable `docs/kb/` knowledge-base contract as part of harmonizing the shared
governance core across the connected repos. This repo is pure-stdlib Python, so the
crib-sheet half starts empty **by design** — the journal + contract are the value here. No
tool sheets seeded (nothing non-obvious to document yet); add one only on a real gotcha, and
never import another repo's stack sheets. — claude
